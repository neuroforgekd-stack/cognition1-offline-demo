Serve locally: python3 -m http.server 8080 --bind 0.0.0.0
Run from the repo root so `index.html` is served from the project directory.
Get your Mac IP: `ipconfig getifaddr en0` (or `ipconfig getifaddr en1`)
iPhone URL format: http://<MAC_IP>:8080/index.html
Ensure your iPhone is on the same Wi‑Fi network as the Mac.
Allow camera and microphone when Safari prompts (tap "Allow").